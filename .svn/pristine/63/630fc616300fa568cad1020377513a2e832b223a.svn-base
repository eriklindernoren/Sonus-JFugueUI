package Projekt.Frame;

/**
* The GridListener class keeps track and adds the classes which needs to know about the information needed to enable saving
* and loading of the grid. We decided to use the Observer Design Patterns for this project but do not have need for
* the unregister method so hence it isn't used anywhere in our code.
*
 */

interface GridListener {
    public void register(GridObserver o);
    public void unregister(GridObserver o);
    public void notifyObserver();
}
