package Projekt.Frame;

/**
* The RectangleListener class keeps track and adds the classes which needs to know about the information change
* related to new rectangles in the grid.We decided to use the Observer Design Patterns for this project but do not have need for
* the unregister method so hence it isn't used anywhere in our code.
*
 */


interface RectangleListener {
    public void register(RectangleObserver o);
    public void unregister(RectangleObserver o);
    public void notifyObserver();
}
